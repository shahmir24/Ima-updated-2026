/**
 * The iMA logo files, derived from the approved master `ima-logo.png` at the
 * repository root by cropping, padding and resizing only.
 *
 * The lockup carries its own white "iMA" wordmark, so it is only for the app's
 * dark surfaces, and no separate "iMA" text should sit beside it.
 */
export const IMA_LOCKUP_SRC = '/brand/ima-lockup.png';

/** The infinity mark alone, for spaces too small for the wordmark. */
export const IMA_MARK_SRC = '/brand/ima-mark.png';
